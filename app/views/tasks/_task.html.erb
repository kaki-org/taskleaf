<tr id="<%= dom_id(task) %>">
  <td><%= link_to task.name, task %></td>
  <td><%= task.created_at %></td>
  <td>
    <%= link_to '編集', edit_task_path(task), class: 'btn btn-primary' %>
    <%= link_to '削除', task_confirm_destroy_path(task), class: 'btn btn-danger delete' %>
  </td>
</tr>
